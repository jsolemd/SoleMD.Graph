{
  "_generated": "DO NOT EDIT - Generated by: solemd mcp-sync (source: SoleMD.Infra/mcp/config/)",
  "servers": {
    "context7": {
      "type": "http",
      "url": "https://mcp.context7.com/mcp",
      "instructions": "Fallback external library documentation lookups only. For SoleMD work, always try CodeAtlas docs first: resolve_library_id -> search_docs (use library_ids for cross-library search) -> read_doc/expand_doc_chunk. Use Context7 only when CodeAtlas lacks the library, the indexed docs are stale/unavailable, or the local result quality is clearly insufficient; state that fallback reason explicitly."
    },
    "openaiDeveloperDocs": {
      "type": "http",
      "url": "https://developers.openai.com/mcp",
      "instructions": "Official OpenAI developer documentation. Use for current guidance about OpenAI APIs, products, and Codex."
    },
    "sure-finance": {
      "type": "http",
      "url": "http://localhost:3800/mcp",
      "instructions": "Sure personal-finance MCP for Jon's self-hosted household dashboard. The stack is on-demand: if unavailable, run `solemd finance up` before retrying. Use it to inspect accounts, transactions, holdings, balance sheets, income statements, budgets, goals, tags, and categories only when Jon asks for finance work. Treat all results as sensitive personal financial data. Read operations are allowed within the requested task; call mutating tools (imports, creates, or updates) only when Jon explicitly requests the corresponding change. Authentication is OAuth via `codex mcp login sure-finance`."
    },
    "chrome-devtools": {
      "command": "npx",
      "args": [
        "-y",
        "chrome-devtools-mcp@latest",
        "--browserUrl=http://localhost:9222"
      ]
    },
    "codeatlas": {
      "type": "http",
      "url": "http://localhost:8100/mcp/solemd.graph",
      "instructions": "Primary MCP surface for CodeAtlas navigation in the active project context. Canonical code tools: search_code, inspect_symbol, trace_flow, file_context, dependents, analyze_impact, analyze_diff, graph_overview, list_flows, get_flow, slice_build, find_clones, find_patterns, index_status, refactor_preview, manage_decision. Canonical docs tools: resolve_library_id, search_docs, expand_doc_chunk, list_doc_libraries, read_doc. The Infra namespace additionally advertises query_log_report and docs administration tools. Folded aliases are not advertised: use find_patterns(pattern=bridge_functions), graph_overview(report=true), slice_build(slice_id=...), and search_docs(library_ids=[...]). Default exploration flow: search_code(mode=auto, detail=skeleton, prefer_source=true) -> inspect_symbol(symbol_ref=...) -> trace_flow(from_ref=..., to_ref=...) when a path question appears. For external docs: resolve_library_id -> search_docs -> read_doc or expand_doc_chunk. Context7/web are fallback-only when CodeAtlas lacks the library, is stale/unavailable, or gives clearly low-quality results. Use detail=card to screen and detail=full after selecting the target. A result carrying truncated=true showed only part of its symbol: `lines` is what was returned and `full_lines` is the whole span, so read the rest before concluding what the code does not do. Read matched_on, rank_explain, evidence_card.verifier, graph_context, and unresolved telemetry before broadening. next_action reports weak evidence but never declares a search finished. search_code confidence has three values and no high: low means the query scored where subjects absent from the corpus land, medium means something was measured, unknown means no instrument reached the top row; stats.evidence.basis names the signals used and is empty when it abstained, so branch on basis rather than the label. Rank is relative to the returned set, so an absent subject still ranks first against unrelated code and a thin wrapper ranks first by sharing a name; treat the top hit as a neighbour until inspect_symbol confirms ownership. Use index_status reindex_mode=all|content|graph to target rebuild scope. Cross-project access stays project-scoped via codeatlas-graph, codeatlas-web, codeatlas-make, and codeatlas-infra."
    },
    "neo4j": {
      "command": "/workspaces/SoleMD.Infra/scripts/neo4j-mcp-server.sh",
      "env": {
        "NEO4J_URI": "bolt://localhost:7687",
        "NEO4J_USERNAME": "neo4j",
        "NEO4J_PASSWORD": "solemd.infra",
        "NEO4J_DATABASE": "neo4j"
      },
      "instructions": "Neo4j graph database — direct Cypher queries via official Neo4j MCP server. Use for schema inspection (get-schema), read queries (read-cypher), and write operations (write-cypher). Code-search handles LLM-facing structural workflows via inspect_symbol, trace_flow, and analyze_impact. Use this server for direct ad-hoc Cypher."
    },
    "codeatlas-graph": {
      "type": "http",
      "url": "http://localhost:8100/mcp/solemd.graph",
      "instructions": "CodeAtlas for SoleMD.Graph (biomedical knowledge graph). Use when you need to find or understand Graph components, pages, API routes, hooks, motion/UI patterns, data fetch flows, or related external library docs through the unified code+docs MCP surface."
    },
    "codeatlas-web": {
      "type": "http",
      "url": "http://localhost:8100/mcp/solemd.web",
      "instructions": "CodeAtlas for SoleMD.Web (public website and lecture catalog). Use for Next.js routes, Mantine/Tailwind brand primitives, Motion, lecture metadata, components, tests, and related external library documentation."
    },
    "codeatlas-make": {
      "type": "http",
      "url": "http://localhost:8100/mcp/solemd.make",
      "instructions": "CodeAtlas for SoleMD.Make (markdown-first document generation). Use when you need to find or understand Pandoc, Typst, Carbone, slides, citation/Zotero workflows, filters, templates, document source conventions, or related external library docs through the unified code+docs MCP surface."
    },
    "codeatlas-infra": {
      "type": "http",
      "url": "http://localhost:8100/mcp/solemd.infra",
      "instructions": "CodeAtlas for SoleMD.Infra (shared infrastructure). Use when you need to find or understand Docker Compose, MCP servers, native WSL2 workspace tooling, monitoring, shared operational scripts, or related external library docs through the unified code+docs MCP surface."
    }
  }
}
