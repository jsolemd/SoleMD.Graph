<!-- DO NOT EDIT - Generated by: solemd prompt-sync (source: /workspaces/SoleMD.Infra/prompts/projects.json) -->
# SoleMD.Graph - Local Claude Addendum

Local-only overrides go here. Project rules live in `../CLAUDE.md`, generated from `../PROJECT_INSTRUCTIONS.md`.

<!-- REPOWISE:START — Do not edit below this line. Auto-generated by Repowise. -->
## Codebase Intelligence for SoleMD.Graph (Repowise)

Indexed by [Repowise](https://repowise.dev). Last indexed: 2026-08-21 (commit 1b81fbd). Confidence: 100%.
### How to work in this repo

- **Trust the index.** `verified: true` means the bytes were checked against the live tree, so never re-read those lines. Re-read only on `bounds: "approximate"`, `_meta.stale_warning`, `search_method: "bm25"` or `confidence: "low"`; `index_behind: true` alone is informational.
- **Pre-edit, not instead-of-edit.** These tools decide *which* files to read and edit. Claude Code requires a raw Read of any file you will Edit, and that Read is correct and expected.
- **Noisy commands** (tests, builds, `git log`/`diff`, searches, listings): prefer `repowise distill <cmd>` — same command, exit code preserved, errors-first output. A `[repowise#<ref>: N lines omitted]` marker is recoverable via `repowise expand <ref>` (add `-q <regex>` to filter); never re-run the command to see omitted output.
- **Recording a decision** you had to reason out: `repowise decision add --title T --decision D` records it without prompting and prints the id (`--format json` to parse it back). It lands `proposed`, for a person to confirm.

### Tools

| Tool | When and why |
|------|--------------|
| `get_answer(question)` | First call for any how/where/why question. Cite `confidence: "high"` or `grounding: "extracted"` directly; `degraded` means judge by `retrieval_quality`. `symbol_bodies` has live bodies. |
| `get_context(targets=[...])` | Triage card for files/modules/symbols: docs, signatures, hotspot, fix history. No source bytes — `include=["skeleton"]` for the whole file verified, `["callers"|"decisions"]` for depth. Batch targets. |
| `get_symbol(id)` | **Follow-up, not an entry point** — one verified body for an id a prior response named (`path.py::Name`, `path.py:140-180`, `repowise#<hex>`). Never walk a file symbol by symbol; Read it. |
| `search_codebase(query)` | Hybrid search, auto-routed by query shape; force with `mode=symbol|path|concept|hybrid`. A hit whose `sources` are `[fts]` only has no semantic agreement, so verify it. |
| `get_why(query, targets?)` | Why the code is shaped this way: decision records, git archaeology, rationale comments. Call before a refactor or a pattern divergence. |
| `get_risk(targets, changed_files?)` | What history says about touching these files. PR mode (`changed_files`) leads with a `directive`: read `will_break` / `missing_cochanges` / `missing_tests` / `tests_to_run` first. |
| `get_change_risk(revspec, extensions?, exclude_patterns?)` | Defect score for a whole commit or `base..head` range, from its diff on the live checkout. Lead with `risk_percentile`. Scores a range; `get_risk` scores paths. |
| `get_health(targets?, include?)` | Defect / maintainability / performance scores and findings. Self-check the files you touched before finishing. |
| `get_dead_code()` | Confidence-tiered unreachable files / unused exports / zombie packages. For cleanup sweeps, not targeted fixes. |
| `get_overview()` | Architecture map. Call once, first, in an unfamiliar repo; skip it after that. |

### Architecture
**Files:** 1492 | **Lines:** 410071 | **Monorepo:** 5 packages | **Import cycles:** 7
SoleMD.Graph is a multi-package typescript codebase of 1492 files, split across 5 packages. Execution starts at apps/api/app/main.py, apps/web/.storybook/main.ts, apps/worker/app/main.py and 6 other entry points. Start here when reading the codebase. Ranked by PageRank over the import graph: the files most of the codebase ultimately depends on.

### Key modules
- `packages` — packages/api-client · packages/api-client/src · packages/api-client/src/client · packages/api-client/src/server ·…
- `apps/web/features/graph/components` — apps/web/features/graph/components/canvas · apps/web/features/graph/components/chrome · apps/web/features/graph/components/entities ·…
- `apps/web/features/wiki` — apps/web/features/wiki/components · apps/web/features/wiki/components/elements · apps/web/features/wiki/components/entity-profiles ·…
- `packages/graph` — packages/graph · packages/graph/src · packages/graph/src/cosmograph · packages/graph/src/cosmograph/hooks ·…
- `packages/graph/src/types` — packages/graph/src/types
**Language:** typescript | **Files:** 8 | **Public symbols:** 39 / 39
Covers the 8 source files in…
- `apps/web/features/orb` — apps/web/features/orb/bake · apps/web/features/orb/chrome · apps/web/features/orb/edges · apps/web/features/orb/interaction ·…
- `packages/api-client` — packages/api-client · packages/api-client/src · packages/api-client/src/client · packages/api-client/src/server ·…
- `apps/web/features/graph/lib/fetch` — apps/web/features/graph/lib/fetch · apps/web/features/graph/stores · apps/web/features/graph/stores/slices
**Language:** typescript |…
- `apps/web/features/graph/lib` — apps/web/features/graph/lib
**Language:** typescript | **Files:** 26 | **Public symbols:** 178 / 230
Covers the 26 source files in…
- `apps/worker/app/corpus` — apps/worker/app/corpus
**Language:** python | **Files:** 26 | **Public symbols:** 210 / 298
Covers the 26 source files in…

### Entry points
- `apps/api/app/main.py`
- `apps/web/.storybook/main.ts`
- `apps/worker/app/main.py`
- `apps/worker/app/telemetry/bootstrap.py`
- `apps/worker/app/ingest/cli.py`
- `apps/worker/app/evidence/cli.py`

### Files that need care (bug-fix history first, then churn — check `get_risk` before editing)
- `apps/web/features/orb/webgpu/orb-webgpu-shader.ts` — 5 bug fixes, last fix 4 months ago (bug magnet); 0 commits/90d

### Code health
Three co-equal signals: defect risk 8.53/10 avg, hotspot health 8.53/10 (stable), worst `apps/worker/app/ingest/runtime.py` at 1.0/10 · maintainability 8.29/10 · performance risk 104 open static I/O-in-loop / N+1 findings. Detail: `get_health()`.

Critical files:
- `apps/web/features/graph/components/panels/prompt/use-rag-query.ts` — brain method (useRagQuery) — impact −1.0
- `apps/web/features/graph/components/panels/editor/use-create-editor-controller.ts` — brain method (useCreateEditorController) — impact −1.0
- `apps/web/features/graph/components/explore/info-panel/use-info-widget-data.ts` — brain method (useInfoWidgetData) — impact −1.0
- `apps/web/features/field/controller/BlobController.ts` — brain method (tick) — impact −1.0
- `apps/worker/app/pmc_fulltext/parse_bioc.py` — brain method (parse_pmc_bioc_fulltext) — impact −0.6

### Commands
- Build: `npm run build`
- Test: `npm run test`
- Lint: `npm run lint`
- Dev: `npm run dev`
- Typecheck: `npm run typecheck`

<!-- REPOWISE:END -->
