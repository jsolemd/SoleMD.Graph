"""Corpus definition: S2 exploration, domain filtering, PubTator loading.

Modules are invoked directly via `python -m app.corpus.<module>`.
Lazy imports only — eager imports cause RuntimeWarning when running
submodules as __main__.
"""
