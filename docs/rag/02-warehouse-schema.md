# 02 — Warehouse Schema

> **Status**: locked for table inventory, identity contract, partitioning plan,
> and per-family write/read patterns. Microdesign values (partition count,
> fillfactor tiers, TOAST mode per column, index set) are **provisional until
> the first sample build** validates them against real data.
>
> **Date**: 2026-04-16
>
> **Scope**: the canonical warehouse cluster (`graph-db-warehouse`). Serving
> projections, graph-serving tables, and API projection tables live on the
> serve cluster and are specified in `03-serve-schema.md`.
>
> **Schema authority**: this document is the PG-native authority. Every
> consumer — engine asyncpg code, Pydantic boundary models, and the SQL-first
> schema / migration surfaces under `db/schema/warehouse/` and
> `db/migrations/warehouse/` — derives from here, not the other way around.

## Purpose

Define the fastest, most scalable PG 18 warehouse schema possible for a
14 M-paper biomedical knowledge graph with clean-slate authoring, so that
every downstream surface (ingest, projection, serving) resolves its types,
keys, and joins against the same contract.

## 0. Global conventions

Everything under this heading applies to every table in the warehouse
unless a table explicitly states otherwise.

### 0.1 Types

| Concern | Choice | Rationale |
|---|---|---|
| Time | `timestamptz` universally | UTC-anchored, locale-safe, no `timestamp` ambiguity. |
| Variable text | `text` only; never `varchar(n)` | `varchar(n)` has no PG performance benefit and introduces schema churn on length changes. |
| Semi-structured | `jsonb` only; never `json` | Binary storage, indexable, fast access. |
| Dates without time | `date` | Source-native precision when the upstream only gives a date. |
| Money / exact decimal | `numeric` | None today; reserved. |
| Scores / weights | `real` (single precision) | Ranking signals don't need double precision; halves storage. |
| Dense embeddings | `halfvec(n)` (pgvector) | 50 % storage vs `vector(n)`, recall parity at 768 dims, 29× faster parallel HNSW build. Provisional pending sample. |
| Enums | `smallint` with constant mapping in the engine code, **not** PG `ENUM` | PG `ENUM` adds DDL on every new value; `smallint` + named constants is cheaper and more portable. |
| Bit flags | `bit(n)` | Compact source-mask style bitfields (`source_mask`). |

### 0.2 Identity

| Identity | Type | Generator | Lives on |
|---|---|---|---|
| `corpus_id` | `bigint` | `IDENTITY GENERATED BY DEFAULT AS IDENTITY` | every fact table keyed to a paper |
| `concept_id` | `bigint` | identity | `concepts`, fact tables |
| `venue_id`, `author_id` | `bigint` | identity | registries |
| `source_release_id` | `integer` | identity (low cardinality) | `source_releases`, fact tables |
| `ingest_run_id` | `uuid` (v7) | `uuidv7()` (PG 18 native) | `ingest_runs`, `load_history` |
| `paper_text_run_id` | `uuid` (v7) | `uuidv7()` | `paper_text_acquisition_runs` |
| `graph_run_id` | `uuid` (v7) | `uuidv7()` | `graph_runs` |
| `chunk_version_key` | `uuid` (v7) | `uuidv7()` | `paper_chunk_versions` |
| `api_projection_run_id`, `serving_run_id` | `uuid` (v7) | `uuidv7()` | serve cluster (in `03`) |
| `evidence_key` | `uuid` (v5) | deterministic `uuidv5(solemd_ns, …)` | `paper_evidence_units` |

Rationale:
- `BIGINT` everywhere rows are counted in the tens of millions or more and
  frequently join — tight (8 B), sequential, cache-friendly.
- `UUIDv7` on run / version identities that cross system boundaries
  (projection jobs, serving packages, graph builds) so they're
  timestamp-ordered and externally sortable without leaking internal
  sequence values.
- `UUIDv5` on `evidence_key` so grounding identity is content-bound,
  release-independent, and round-trippable into PG — the whole point per
  `rag-future.md` Identity Glossary.

### 0.3 Column layout (MAXALIGN ordering)

Every table orders columns in fixed-width-descending → variable-last:

```
8-byte (BIGINT, DOUBLE PRECISION, TIMESTAMPTZ, UUID)
  → 4-byte (INT, REAL, DATE)
    → 2-byte (SMALLINT)
      → 1-byte (BOOL, CHAR, "char")
        → variable (TEXT, JSONB, arrays, vectors, tsvector, bit varying)
```

At 100 M-row scale this saves 8–16 bytes per tuple (0.8–1.6 GB per 100 M
rows) on alignment padding. Worth doing once, on a fresh build.

### 0.4 TOAST & compression

- Cluster-wide: `default_toast_compression = lz4`. LZ4 is 2–4× faster
  decompression than pglz with similar ratios for English prose.
- `text` columns inherit `EXTENDED` (compress + TOAST out-of-line).
- Large prose columns should usually stay `EXTENDED` and use LZ4 compression;
  this cuts heap/TOAST footprint without changing application semantics.
- Reserve `STORAGE EXTERNAL` for already-incompressible payloads, or cases
  where measurement shows decompression dominates and the extra storage/WAL
  cost is acceptable.
- `toast_tuple_target` stays at the PG 18 default (2 kB) except where a
  single wide table has one rarely-read large column we want to keep off
  the main heap page.

### 0.5 Fillfactor tiering

| Tier | Value | Applies to |
|---|---:|---|
| Append-mostly large facts | 100 | `paper_citations`, `paper_citation_contexts`, `paper_concepts`, `paper_relations`, `paper_blocks*`, `paper_sentences*`, `paper_*_mentions*`, `paper_chunk_members*`, `pubtator.*` |
| Update-occasional | 90 | `papers`, `paper_text`, `paper_lifecycle`, `paper_metrics`, `concepts`, `concept_aliases`, `concept_search_aliases` |
| Status-flipping control | 80 | `source_releases`, `ingest_runs`, `paper_chunk_versions`, `graph_runs`, `graph_bundle_artifacts` |

Rationale: HOT updates need free space on the same page; append-mostly
tables don't benefit from fillfactor < 100, and going below 80 is
empirically worse than 80 in modern PG.

### 0.6 Partitioning

- **Hash partition count**: 32 for every `corpus_id`-keyed fact family.
  32 × (PK + 1–2 local indexes) stays within PG 18's 64-slot fast-path
  lock table; 64 partitions × 3 indexes lands in the main lock table and
  produces `LWLock:LockManager` contention on this box's 24 logical
  cores. Provisional pending sample.
- **No partitioning** for small / bounded tables (`corpus`, `papers`,
  `paper_text`, `venues`, `authors`, `concepts`, `graph_runs`, etc.) —
  they never grow past a few tens of millions of rows, fit comfortably in
  a single table, and partitioning would only add planner cost.
- **Time-range partitioning** (`range` on a `timestamptz` or
  `source_release_id`): considered for `ingest_runs` and `load_history`
  if they grow past ~10 M rows. Today they don't; revisit via
  `pg_partman` 5.x when measured.

### 0.7 Indexing principles

- PK on every table. No heap-only tables.
- One canonical forward index per join direction. One canonical reverse
  index when both directions are hot.
- Partial indexes for hot filter predicates (`is_influential = true`,
  `retraction_status = 'active'`, `is_active = true`, `is_primary = true`).
- GIN for `tsvector`; GiST + `gin_trgm_ops` for trigram fuzzy lookup on
  small canonical alias tables.
- BRIN on append-time-ordered columns with physical correlation
  (`source_releases.source_ingested_at`, `ingest_runs.started_at`) —
  cheap and effective on analytical scans.
- `INCLUDE` covering indexes only on serve-side list queries
  (they live in `03-serve-schema.md`, not here).
- No speculative ANN indexes in PG (per `rag-future.md` §1516 — the
  serving plane is OpenSearch). Warehouse `paper_embeddings_retrieval`
  gets no HNSW by default.

### 0.8 Schemas

Four schemas only:

- `solemd` — canonical warehouse, grounding, concepts, build control.
- `pubtator` — raw PubTator3 ingest surfaces.
- `umls` — raw UMLS Metathesaurus reference tables.
- `solemd_scratch` — run-scoped UNLOGGED corpus-selection artifacts. Durable
  resume state stays in logged `solemd.corpus_selection_artifacts`; this schema
  only holds rebuildable scratch tables.

No `raw`, `stage`, or `archive` schemas — the `s2_*_raw` tables already
carry the raw-ingest role, and stage-and-swap uses `_next` / `_old`
suffixes on the same schema.

### 0.9 Tablespaces

**Do not create tablespaces inside either cluster.** Per `00-topology.md`,
storage isolation is the responsibility of the two-cluster split, not of
tablespaces within a cluster.

### 0.10 Comments and migrations

- `db/schema/enum-codes.yaml` is the authoritative machine-readable
  registry for every SMALLINT-coded enum used by warehouse and serve.
  `engine/app/models/shared/enums.py` and the generated SQL
  column-comment artifact are **exclusively generated from it**; docs
  use symbolic names for readability, but on-disk values are SMALLINT
  codes from the registry.
- Every table and every non-obvious column carries a `COMMENT ON …` so
  `\d+` output and tooling stay legible.
- Ordered PostgreSQL SQL under `db/schema/warehouse/` is the authoring surface;
  `scripts/schema_migrations.py` remains the executor ledger
  per `12-migrations.md`.

## 1. Extensions

Required in the initial warehouse baseline:

```sql
CREATE EXTENSION IF NOT EXISTS pg_trgm;           -- trigram GiST / GIN
CREATE EXTENSION IF NOT EXISTS pgcrypto;          -- digest, hmac helpers
CREATE EXTENSION IF NOT EXISTS pg_stat_statements;-- baseline observability
CREATE EXTENSION IF NOT EXISTS pg_buffercache;    -- warmup + hit-ratio diag
```

Deferred from the initial warehouse baseline until the warehouse image/config
slice lands or measured row-volume pressure justifies them:

```sql
CREATE EXTENSION IF NOT EXISTS vector;            -- halfvec, vector, ivfflat, hnsw
CREATE EXTENSION IF NOT EXISTS hypopg;            -- plan experiments
CREATE EXTENSION IF NOT EXISTS pg_cron;           -- scheduled VACUUM, refresh
CREATE EXTENSION IF NOT EXISTS pg_partman;        -- only if time-range lifecycle kicks in
```

`postgres_fdw` is **not** installed on the warehouse cluster. FDW lives
on the serve cluster reading *into* warehouse; the warehouse cluster is
always the source, never the querier.

`auto_explain` is a preload module, not a `CREATE EXTENSION` surface.
Load it via `shared_preload_libraries` / `session_preload_libraries` and tune
`auto_explain.*` in `09-tuning.md`. Day-one UUIDv5 generation for
`evidence_key` is engine-owned; if DB-side UUIDv5 audit helpers are later
needed, add `uuid-ossp` explicitly rather than assuming a core `uuidv5()`
function.

## 2. Identity glossary (warehouse-scoped)

| Identity | Meaning | Lifetime | Generation |
|---|---|---|---|
| `corpus_id` | Canonical paper identity across the entire SoleMD estate. | Permanent. Never recycled. | Identity sequence on `solemd.corpus.corpus_id`. Every other warehouse table that references a paper carries `corpus_id`, not `pmid` / `doi` / `s2_paper_id`. |
| `source_release_id` | Surrogate PK for one source release (e.g. S2 `2026-03-10`). | Permanent. | Identity sequence; low cardinality. |
| `ingest_run_id` | One ingest run against one source release. | Permanent. | `uuidv7()` — timestamp-ordered so `ORDER BY run_id` tracks wall-clock order. |
| `paper_text_run_id` | One targeted full-text acquisition attempt for one `corpus_id`. | Permanent. | `uuidv7()`. |
| `concept_id` | Canonical concept identity across RAG, graph, and API surfaces. | Permanent. | Identity sequence on `solemd.concepts`. Source identifiers (UMLS CUI, MeSH, NCBI Gene) live in `concept_xrefs`. |
| `chunk_version_key` | Version identity for the chunk derivation policy. | Permanent. | `uuidv7()` at policy-change time. |
| `graph_run_id` | Graph build run identity. | Permanent. | `uuidv7()`. |
| `evidence_key` | Content-bound grounding identity. Round-trippable to canonical coordinates via `paper_evidence_units`. | Permanent for a given `chunk_version_key`. | Deterministic `uuidv5(solemd_namespace, corpus_id ‖ kind ‖ section_ord ‖ block_ord ‖ sent_start ‖ sent_end ‖ chunk_version_key)`. |

Derived identities (`api_projection_run_id`, `serving_run_id`) live on the
serve cluster — see `03-serve-schema.md`.

## 3. Partitioning contract

### 3.1 Families

Hash-partitioned by `corpus_id` into 32 children, named `_p00 … _p31`:

- `solemd.paper_blocks`
- `solemd.paper_sentences`
- `solemd.paper_citation_mentions`
- `solemd.paper_entity_mentions`
- `solemd.paper_chunk_members`
- `solemd.paper_chunks` (compat / fallback)
- `solemd.paper_concepts`
- `solemd.paper_relations`
- `pubtator.entity_annotations` (after `pmid → corpus_id` backfill)
- `pubtator.relations` (after backfill)

No current `paper_citations` partitioning is part of the mapped citation
contract. If mapped citation volume later forces partitioning, use the open
trigger in §11 rather than treating broad raw citation scale as the default.

Future/provisional citation-context storage, if needed, may partition by the
mapped citing paper key.

### 3.2 Children

Every child declares:
- Local PK matching the parent.
- `fillfactor = 100` (all partitioned families are append-mostly).
- Local indexes identical in structure across children.
- `autovacuum_enabled = false` **during bulk load only**, re-enabled after.

The canonical SQL schema files under `db/schema/warehouse/` enumerate all
32 children explicitly. This document shows parent + one representative child;
read the schema files for the full set.

### 3.3 Staged partitioning for `pubtator.*`

Until the `pmid → corpus_id` backfill completes, raw PubTator rows lack
a `corpus_id`. Partitioning plan:

1. Initial load into a non-partitioned staging table
   `pubtator.entity_annotations_stage`.
2. Backfill `corpus_id` via join to `solemd.papers(pmid)`.
3. COPY backfilled rows into the partitioned parent
   `pubtator.entity_annotations`.
4. Drop staging after verification.

This avoids a "half the rows don't prune" regression that
list-then-hash nested partitioning would cause.

## 4. Table families

Each subsection gives a one-paragraph purpose, the canonical column
layout (in MAXALIGN order, with types), the keys / indexes, partitioning
(if any), fillfactor, and representative SQL for the parent. Children and
secondary indexes are noted in prose where omitting them keeps the doc
legible.

### 4.1 Raw ingest and release tracking

Raw-ingest tables exist to preserve source-normalized inputs in PG
without making them the runtime surface. They are canonical *rebuild
inputs*, not canonical truth.

#### `solemd.source_releases`

One row per external-source release (S2 monthly drops, PubTator3 weekly,
UMLS quarterly). Fillfactor 80 (status transitions).

`release_status` stays TEXT in the baseline because operators inspect these
rows directly during bootstrap, triage, and migration review; the table is
small and control-plane oriented.

```hcl
table "source_releases" {
  schema = schema.solemd
  column "source_release_id"    { null = false, type = integer, identity { generated = BY_DEFAULT } }
  column "source_published_at"  { null = true,  type = timestamptz }
  column "source_ingested_at"   { null = false, type = timestamptz, default = sql("now()") }
  column "manifest_checksum"    { null = true,  type = text }
  column "manifest_uri"         { null = true,  type = text }
  column "source_name"          { null = false, type = text }
  column "source_release_key"   { null = false, type = text }
  column "release_status"       { null = false, type = text, default = "planned" }  // planned | ingesting | loaded | retired
  primary_key { columns = [column.source_release_id] }
  index "uq_source_releases_natural" {
    unique  = true
    columns = [column.source_name, column.source_release_key]
  }
  index "idx_source_releases_ingested" {
    columns = [column.source_name, column.source_ingested_at]
    // used for "latest release per source" lookups
  }
  settings { fillfactor = 80 }
}
```

#### `solemd.ingest_runs`

One row per ingest or rebuild run. Fillfactor 80. Symbolic lifecycle and
operator-action names below are stored as SMALLINT codes from the enum
registry in §0.10.

Columns (MAXALIGN order):
- `ingest_run_id` UUID, PK, default `uuidv7()`
- `advisory_lock_key` BIGINT
- `started_at`, `completed_at` TIMESTAMPTZ
- `source_release_id` INTEGER, FK → `source_releases`
- `status` SMALLINT (`started` | `loading` | `indexing` | `analyzing` |
  `published` | `failed` | `aborted`)
- `requested_status` SMALLINT (`continue` | `abort`) nullable
- `manifest_uri` TEXT
- `error_message` TEXT
- `last_loaded_family` TEXT
- `families_loaded` TEXT[] default empty array
- `plan_manifest` JSONB
- `phase_started_at` JSONB

Indexes:
- PK on `ingest_run_id`.
- Btree on `(source_release_id, started_at DESC)` — "latest run per
  release."
- Btree on `(status, started_at DESC)` — stuck-run and recent-failure
  operational lookups.
- BRIN on `started_at` — analytical scans.

#### `solemd.ingest_file_tasks`

Durable DB-backed file work queue for ingest families that need safe
intra-family fanout. Current landed use: S2 `citations`.

Columns:
- `ingest_run_id` UUID, FK → `ingest_runs`
- `source_release_id` INTEGER, FK → `source_releases`
- `family_name` TEXT
- `file_name` TEXT
- `file_path` TEXT
- `file_byte_count` BIGINT default `0`
- `status` SMALLINT (`1=pending`, `2=running`, `3=completed`,
  `4=failed`)
- `attempt_count` INTEGER default `0`
- `input_bytes_read` BIGINT default `0`
- `rows_written` BIGINT default `0`
- `stage_row_count` INTEGER default `0`
- `claim_token` UUID nullable — per-claim lease token; file workers must
  present it before heartbeat, stage merge, checkpoint, complete, or fail
  updates
- `enqueued_at`, `started_at`, `completed_at`, `updated_at` TIMESTAMPTZ
- `last_error` TEXT

Keys / indexes:
- PK `(ingest_run_id, family_name, file_name)`.
- Btree `(source_release_id, ingest_run_id, family_name, status,
  updated_at)` — fanout progress, stale reset, and operator inspection.

#### `solemd.s2_dataset_cursors`

One row per Semantic Scholar dataset that SoleMD has accepted as the local
current-state base. Full S2 ingest seeds this table as `base_loaded`. Diff
planning updates `last_diff_checked_at` and `last_diff_plan_checksum`, but it
does not make hot source files delete-safe. `hot_source_delete_safe_at` must
remain null until the tested raw diff-application path for that family exists.

Columns:
- `dataset_name` TEXT, PK (`papers`, `abstracts`, `publication-venues`, ...)
- `family_name` TEXT — SoleMD family registry name
- `base_release_key` TEXT — full-release base
- `current_release_key` TEXT — current local dataset state
- `current_source_release_id` INTEGER, FK → `source_releases`
- `cursor_status` TEXT (`base_loaded`, `diff_planned`, `diff_applying`,
  `diff_loaded`, `blocked`)
- `diff_apply_enabled` BOOLEAN
- `hot_source_delete_safe_at` TIMESTAMPTZ nullable
- `last_diff_checked_at` TIMESTAMPTZ nullable
- `last_diff_plan_checksum` TEXT nullable
- `updated_at` TIMESTAMPTZ

#### `solemd.s2_dataset_diff_manifests` /
#### `solemd.s2_dataset_diff_files`

Durable Semantic Scholar Datasets API diff ledger. The manifest table records
each sequential diff returned by
`/datasets/v1/diffs/{start_release_id}/to/{end_release_id}/{dataset_name}`;
the file table records every update/delete URL for later streamed application.

Keys / indexes:
- `s2_dataset_diff_manifests.s2_diff_manifest_id` UUID PK.
- Unique `(dataset_name, start_release_key, end_release_key, diff_ordinal)`.
- `s2_dataset_diff_files` PK
  `(s2_diff_manifest_id, operation, file_ordinal)`, where operation is
  `update` or `delete`.
- Btree `(operation, file_status, updated_at)` for future worker fanout.

#### `solemd.paper_text_acquisition_runs`

One row per paper-level full-text acquisition attempt. Fillfactor 80.

Columns:
- `paper_text_run_id` UUID, PK, default `uuidv7()`
- `advisory_lock_key` BIGINT
- `corpus_id` BIGINT, FK → `corpus`
- `requested_by` TEXT
- `started_at`, `completed_at` TIMESTAMPTZ
- `status` SMALLINT (`started` | `published` | `unavailable` | `failed`)
- `locator_kind` TEXT nullable (`pmcid` | `pmid`)
- `locator_value` TEXT nullable
- `resolved_pmc_id` TEXT nullable
- `resolver_kind` TEXT nullable
- `manifest_uri` TEXT nullable
- `response_checksum` TEXT nullable
- `error_message` TEXT nullable

Indexes:
- PK on `paper_text_run_id`.
- Btree on `(corpus_id, started_at DESC)` — latest run per paper.
- Btree on `(status, started_at DESC)` — operational failure / backlog scans.

#### `solemd.s2_papers_raw`

Normalized S2 paper metadata. Fillfactor 100. This is a current-state table
keyed by S2 paper id, not a duplicate full snapshot per release; unchanged rows
are not rewritten just to advance an S2 diff cursor.

Columns: `corpus_id` (nullable until canonical corpus materialization), `source_release_id`,
`paper_id` TEXT (S2 paperId), `pmid` INTEGER, `doi_norm` TEXT, `pmc_id`
TEXT, `title` TEXT, `abstract` TEXT, `venue_raw` TEXT, `year` INTEGER,
`publication_date` DATE, `payload_checksum` TEXT, `last_seen_run_id` UUID.

Keys / indexes:
- PK `(paper_id)`.
- Btree `(pmid)` where `pmid IS NOT NULL`.
- Btree `(doi_norm)` where `doi_norm IS NOT NULL`.
- Btree `(pmc_id)` where `pmc_id IS NOT NULL`.
- Btree `(corpus_id)` where `corpus_id IS NOT NULL` — used during backfill.

Rule: raw fields are typed and narrow. We do **not** store giant JSONB
payload blobs as the primary raw representation; the original S2 file
is preserved under `/mnt/solemd-graph/data/semantic-scholar/releases/…`
and referenced by `manifest_uri` on `ingest_runs`.

#### `solemd.s2_paper_authors_raw`

- `paper_id` TEXT, `author_ordinal` INTEGER, `source_author_id` TEXT,
  `name_raw` TEXT, `affiliation_raw` TEXT.
- PK `(paper_id, author_ordinal)`.
- Btree `(source_author_id)`.

#### `solemd.s2_authors_raw`

- `source_release_id` INTEGER, `source_author_id` TEXT, `orcid` TEXT,
  `display_name` TEXT, `last_seen_run_id` UUID.
- PK `(source_release_id, source_author_id)`.
- Btree `(source_author_id)`.
- This is the raw author-registry surface loaded directly from the S2
  `authors/` dataset. Per-paper authorship fanout still lands in
  `s2_paper_authors_raw` from the `papers/` dataset.

#### `solemd.s2_paper_reference_metrics_raw`

- `source_release_id` INTEGER, `citing_paper_id` TEXT,
  `reference_out_count` INTEGER, `influential_reference_count` INTEGER,
  `linked_reference_count` INTEGER, `orphan_reference_count` INTEGER,
  `last_seen_run_id` UUID.
- PK `(source_release_id, citing_paper_id)`.
- Btree `(source_release_id, citing_paper_id)`.
- This is the default broad raw citation surface under the locked stage
  contract. It supports corpus and mapped gating without forcing full
  citation-edge persistence before mapped.

#### `solemd.s2_paper_reference_metrics_file_checkpoints`

Durable per-file completion checkpoints for the S2 citation aggregate path.
The aggregate stage table is `UNLOGGED`, so a checkpoint is trusted only when
the matching stage rows still exist and match `stage_row_count`. If Postgres
restarts and clears the stage, the worker deletes invalid checkpoints and
reprocesses those files through `solemd.ingest_file_tasks`.

Columns:
- `ingest_run_id` UUID, FK → `ingest_runs`
- `source_release_id` INTEGER, FK → `source_releases`
- `file_name` TEXT
- `file_byte_count` BIGINT default `0`
- `stage_row_count` INTEGER default `0`
- `completed_at` TIMESTAMPTZ default `now()`

Key:
- PK `(ingest_run_id, source_release_id, file_name)`.

#### `solemd.s2_paper_references_raw`

- `source_release_id` INTEGER, `citing_paper_id` TEXT,
  `reference_checksum` TEXT, `cited_paper_id` TEXT nullable,
  `linkage_status` SMALLINT (`pending` | `linked` | `orphan`),
  `is_influential` BOOLEAN, `intent_raw` TEXT.
- PK `(source_release_id, reference_checksum)`.
- `reference_checksum` is a release-scoped stable checksum over the upstream
  citation row identity/payload so raw rows can remain refresh-safe even when
  S2 omits `citedcorpusid`.
- Btree `(source_release_id, linkage_status, citing_paper_id)`.
- Reverse btree `(source_release_id, cited_paper_id, citing_paper_id)`.

Transitional note:

- The current broad raw edge table remains documented because it exists in the
  implementation inventory today.
- The locked next implementation batch introduces a broad pre-mapped aggregate
  citation surface carrying at least `reference_out_count` and
  `influential_reference_count`, ideally also `linked_reference_count`.
- Actual citation-edge persistence is mapped-owned enrichment into
  `paper_citations`, not the default broad raw contract. Evidence owns
  in-text citation anchors in `paper_citation_mentions`.

#### `solemd.s2_paper_assets_raw`

- `paper_id` TEXT, `asset_kind` TEXT, `asset_url` TEXT,
  `content_type` TEXT, `availability_raw` TEXT, `asset_checksum` TEXT.
- PK `(paper_id, asset_kind, asset_url)`.
- Btree `(paper_id)`.

Stage-ownership note:

- `pubtator.*_stage` remains the broad raw PT3 substrate.
- Under the locked next stage contract, canonical
  `pubtator.entity_annotations` / `pubtator.relations` become mapped-owned
  surfaces rather than corpus-baseline surfaces for every admitted paper.

#### `pubtator.entity_annotations`

Hash-partitioned by `corpus_id` × 32 after the `pmid → corpus_id`
backfill. Fillfactor 100. Append-mostly per release.

Columns (MAXALIGN order):
- `corpus_id` BIGINT NOT NULL (post-backfill)
- `source_release_id` INTEGER NOT NULL
- `start_offset` INTEGER NOT NULL
- `end_offset` INTEGER NOT NULL
- `pmid` INTEGER (retained for audit and for un-backfilled rows)
- `entity_type` SMALLINT NOT NULL (mapped from PubTator type)
- `mention_text` TEXT NOT NULL
- `concept_id_raw` TEXT NOT NULL (source CUI / MeSH / NCBI Gene)
- `resource` SMALLINT NOT NULL (source mask: PubTator3, etc.)

Local indexes per partition:
- Unique digest key `(corpus_id, start_offset, end_offset, entity_type,
  sha256(concept_id_raw), resource)`; raw `concept_id_raw` remains stored, but
  btree uniqueness never indexes pathological multi-kilobyte source IDs
  directly.
- Btree `(corpus_id, concept_id_raw)` — concept-first scans.
- Btree `(pmid, start_offset)` — used until backfill is complete; can
  drop after.

#### `pubtator.relations`

Hash-partitioned by `corpus_id` × 32. Fillfactor 100.

Columns: `corpus_id`, `source_release_id`, `pmid`, `relation_type` SMALLINT,
`subject_entity_id` TEXT, `object_entity_id` TEXT, `subject_type` SMALLINT,
`object_type` SMALLINT, `relation_source` SMALLINT.

Local indexes per partition:
- Unique digest key `(corpus_id, sha256(subject_entity_id), relation_type,
  sha256(object_entity_id))`; raw endpoint IDs remain stored, but btree
  uniqueness never indexes pathological multi-kilobyte source IDs directly.
- Btree `(corpus_id, object_entity_id, relation_type, subject_entity_id)`.
- Materialization priority: when rows collide on `(corpus_id, subject,
  relation, object)`, `relation_source = 1` is authoritative and
  `relation_source = 2` only fills rows that source 1 did not already provide.

#### `umls.*`

UMLS Metathesaurus raw tables — `MRCONSO`, `MRREL`, `MRSTY`, `MRHIER`,
`MRDEF`. These keep the UMLS source-native schema (not MAXALIGN-reordered)
so a re-load from Metathesaurus drops in cleanly. All are
`fillfactor = 100`. SoleMD-canonical concept derivation lives in
`solemd.concepts` and `solemd.concept_*`, not here.

### 4.2 Canonical metadata & lifecycle

The metadata layer is narrow, typed, and joinable. It is **not** where
text or vectors live — those split out into `paper_text` and
`paper_embeddings_*`.

#### `solemd.corpus`

Stable domain membership. Fillfactor 90.

```hcl
table "corpus" {
  schema = schema.solemd
  column "corpus_id"        { null = false, type = bigint, identity { generated = BY_DEFAULT } }
  column "first_seen_at"    { null = false, type = timestamptz, default = sql("now()") }
  column "last_seen_at"     { null = false, type = timestamptz, default = sql("now()") }
  column "admission_reason" { null = false, type = text }
  column "domain_status"    { null = false, type = text, default = "corpus" }  // corpus | mapped | retired
  primary_key { columns = [column.corpus_id] }
  index "idx_corpus_domain_status" {
    columns = [column.domain_status]
  }
  settings { fillfactor = 90 }
}
```

`domain_status` also stays TEXT in the baseline. It is a low-cardinality review
state surfaced directly to operators during canonical corpus / mapped-universe
work, so the extra enum-code indirection is deferred. `mapped` is a stricter
active-universe subset inside the canonical corpus rather than a second durable
status family.

Operationally:

- broad canonical-corpus membership is `c.domain_status IN ('corpus', 'mapped')`
- mapped paper-level active-universe membership is `c.domain_status = 'mapped'`

Rule: release-scoped flags (`is_in_current_map`, `is_in_current_base`)
that lived in `corpus` previously are removed. Current-run membership
lives on `graph_points` / `serving_members` in the serve cluster.

#### `solemd.papers`

Canonical paper identity + non-text bibliographic metadata. Fillfactor 90.

Columns (MAXALIGN):
- `corpus_id` BIGINT PK FK → `corpus`
- `venue_id` BIGINT FK → `venues`
- `publication_date` DATE
- `year` SMALLINT (denormalized for range scans without casting)
- `language` SMALLINT (enum code)
- `article_type` SMALLINT (enum code)
- `is_open_access` BOOLEAN
- `is_retracted` BOOLEAN NOT NULL DEFAULT false
- `pmid` INTEGER unique-where-not-null
- `doi_norm` TEXT unique-where-not-null
- `pmc_id` TEXT unique-where-not-null
- `s2_paper_id` TEXT unique-where-not-null
- `created_at`, `updated_at` TIMESTAMPTZ

Indexes:
- PK `(corpus_id)`.
- Unique partial btrees on each external id (pmid, doi, pmc, s2).
- Btree `(venue_id, year)` only if query families actually need it
  (provisional — drop if not used).

#### `solemd.paper_text`

Canonical text storage, split out from `papers` so metadata reads don't
drag text payloads off disk. Fillfactor 90.

Columns (MAXALIGN):
- `corpus_id` BIGINT PK FK → `corpus`
- `title_hash` BYTEA (16 B SHA-1 prefix of normalized title — exact-match fast path)
- `abstract_hash` BYTEA
- `text_availability` SMALLINT (enum: none | abstract | fulltext)
- `title` TEXT NOT NULL
- `normalized_title_key` TEXT NOT NULL
  `GENERATED ALWAYS AS (solemd.normalize_title_key(title)) STORED`
- `abstract` TEXT
- `tldr` TEXT
- `fts_vector` TSVECTOR `GENERATED ALWAYS AS (
    setweight(to_tsvector('english', coalesce(title, '')), 'A') ||
    setweight(to_tsvector('english', coalesce(abstract, '')), 'B')
  ) STORED`

Indexes:
- PK `(corpus_id)`.
- Btree `(normalized_title_key)` — exact title matching.
- GiST `(title gist_trgm_ops)` — fuzzy title lookup (deferred until
  proven needed).
- GIN `(fts_vector)` with `fastupdate = on`, `gin_pending_list_limit =
  16 MB`. Post-bulk-load runs `gin_clean_pending_list()` before
  publishing.
- Btree `(text_availability, corpus_id)` — partial for
  `text_availability > 0` to find text-bearing papers.

#### `solemd.venues`

- `venue_id` BIGINT identity PK.
- `issn`, `eissn` TEXT.
- `display_name`, `normalized_name` TEXT.
- `curated_family` SMALLINT (enum) — for graph / retrieval priors.
- Fillfactor 90. Unique btree on `normalized_name`.

#### `solemd.authors`

- `author_id` BIGINT identity PK.
- `orcid` TEXT unique-where-not-null.
- `display_name`, `normalized_name` TEXT.
- Fillfactor 90. Btree on `normalized_name`.
- Unique btree on `source_author_id`.
- Unique partial btree on `normalized_name WHERE source_author_id IS NULL`
  serializes anonymous S2 author fallback upserts during parallel mapped
  materialization. It does not collapse named S2 author identities with source
  ids.

#### `solemd.paper_authors`

Mapped-ownership note:

- Under the locked next stage contract, `paper_authors` is a mapped-owned
  canonical fanout surface. Broad raw author helpers may still exist, but
  canonical author materialization is no longer a corpus-baseline requirement.

- `corpus_id` BIGINT FK → `corpus`.
- `author_id` BIGINT FK → `authors`.
- `author_ordinal` SMALLINT NOT NULL.
- `affiliation_text` TEXT (nullable, EXTERNAL).
- PK `(corpus_id, author_ordinal)`. Reverse btree `(author_id, corpus_id)`.
- Fillfactor 100 (append-only once publication is canonical).

#### `solemd.paper_assets`

- `corpus_id`, `asset_kind` SMALLINT, `asset_uri` TEXT, `source_name` SMALLINT,
  `availability_state` SMALLINT, `rights_class` SMALLINT,
  `license_kind` SMALLINT, `excerpt_eligible` BOOLEAN,
  `fulltext_eligible` BOOLEAN, `asset_checksum` TEXT.
- PK `(corpus_id, asset_kind, asset_uri)`. Btree `(corpus_id)`.
- Fillfactor 100.

#### `solemd.paper_lifecycle`

Retraction / correction / serving-eligibility governance. Fillfactor 90.

Columns: `corpus_id` PK, `first_seen_source_release_id` INTEGER,
`last_seen_source_release_id` INTEGER, `last_refreshed_at` TIMESTAMPTZ,
`retraction_status` SMALLINT, `retracted_at` TIMESTAMPTZ,
`correction_status` SMALLINT, `correction_source` TEXT,
`tombstone_status` SMALLINT, `tombstone_reason` SMALLINT,
`serving_rights_class` SMALLINT, `serving_eligibility_class` SMALLINT.

Indexes:
- PK.
- Partial btree `(serving_eligibility_class, corpus_id)` where
  `serving_eligibility_class > 0` — evidence-lane admission queries.
- Btree `(retraction_status, corpus_id)` where `retraction_status > 0`
  — retraction audit.
- Btree `(last_seen_source_release_id, corpus_id)` — stale-paper scans.

### 4.3 Canonical concepts & references

One canonical concept surface, fed by UMLS + PubTator + curated vocab.

#### `solemd.concepts`

Fillfactor 90.

Columns: `concept_id` BIGINT identity PK,
`preferred_display_name` TEXT NOT NULL,
`normalized_preferred_name` TEXT
  `GENERATED ALWAYS AS (solemd.normalize_concept_name(preferred_display_name)) STORED`,
`semantic_group` SMALLINT, `concept_kind` SMALLINT,
`primary_namespace` SMALLINT, `primary_source_id` TEXT,
`is_active` BOOLEAN NOT NULL DEFAULT true.

Indexes:
- PK.
- Unique `(primary_namespace, primary_source_id)` — e.g. `(UMLS, C0004238)`.
- Btree `(semantic_group, concept_id)` — group filters.
- Btree `(normalized_preferred_name)` — exact lookup by normalized form.

#### `solemd.concept_aliases`

Fillfactor 90.

Columns: `concept_id` BIGINT FK, `alias_text` TEXT, `normalized_alias` TEXT
GENERATED, `alias_kind` SMALLINT, `alias_source` SMALLINT,
`priority_weight` REAL.

Indexes:
- PK `(concept_id, normalized_alias)`.
- Btree `(normalized_alias, concept_id)` — alias → concept lookup.
- GiST `(alias_text gist_trgm_ops)` only if fuzzy alias lookup is a
  proven hot path (deferred).

#### `solemd.concept_search_aliases`

Filtered canonical-support table that feeds the OpenSearch synonym
artifact. Fillfactor 90.

Columns:
- `concept_id` BIGINT FK
- `synonym_version` INTEGER NOT NULL
- `alias_source_priority` SMALLINT
- `observed_mention_count` INTEGER
- `ambiguity_score` REAL
- `alias_source` SMALLINT, `language` SMALLINT
- `eligible_for_search_synonym` BOOLEAN NOT NULL DEFAULT false
- `alias_text` TEXT
- `normalized_alias` TEXT GENERATED
- `search_equivalence_class` TEXT

Indexes:
- PK `(concept_id, normalized_alias, synonym_version)`.
- Partial btree `(synonym_version, normalized_alias, concept_id)` where
  `eligible_for_search_synonym = true` — this is the index the
  synonym-artifact builder scans.
- Btree `(normalized_alias, ambiguity_score, concept_id)`.

Rule: this is a derived table. UMLS is the main canonical source;
PubTator provides observed-surface-form evidence and ambiguity /
frequency support. Only `eligible_for_search_synonym = true` rows feed
the OpenSearch synonym artifact.

#### `solemd.concept_xrefs`

- `concept_id` FK, `xref_namespace` SMALLINT, `xref_value` TEXT.
- Unique `(xref_namespace, xref_value)`. Btree `(concept_id)`.

#### `solemd.concept_relations`

- `source_concept_id`, `relation_type` SMALLINT, `target_concept_id`,
  `source_mask` BIT(8), `confidence` REAL.
- PK `(source_concept_id, relation_type, target_concept_id)`.
- Reverse btree `(target_concept_id, relation_type, source_concept_id)`.

#### `solemd.vocab_terms` + `solemd.vocab_term_aliases`

Curated editorial inputs. Feed `concepts` / `concept_aliases` / `concept_xrefs`
on refresh. Kept as warehouse-local materializations of the repo-curated TSV
assets rather than as serve tables.

`solemd.vocab_terms`

- PK `term_id UUID`.
- Core columns: `canonical_name`, generated `normalized_name`, `category`,
  `umls_cui`, `rxnorm_cui`, `semantic_types TEXT[]`, `semantic_groups TEXT[]`,
  `organ_systems TEXT[]`.
- Operational columns: `source_asset_sha256`, `loaded_at`.
- Indexes:
  - btree `(normalized_name)`
  - btree `(category)`
  - partial btree `(umls_cui)` where `umls_cui IS NOT NULL`

`solemd.vocab_term_aliases`

- PK `(term_id, alias)` with FK to `vocab_terms`.
- Core columns: `alias`, generated `normalized_alias`, `alias_type`,
  `quality_score`, `is_preferred`, `umls_cui`.
- Operational columns: `source_asset_sha256`, `loaded_at`.
- Indexes:
  - btree `(normalized_alias, term_id, quality_score DESC, is_preferred DESC)`
    for PubTator surface joins
  - partial btree `(umls_cui)` where `umls_cui IS NOT NULL`

#### `solemd.corpus_selection_runs` + `solemd.corpus_selection_signals`

The durable run/provenance ledger for release-pair corpus selection.

`corpus_selection_runs`

- One row per `(s2_release_tag, pt3_release_tag, selector_version)` plan run.
- Columns: advisory lock, source release ids, selector version, requester,
  trigger, lifecycle status, error, `phases_completed`, `last_completed_phase`,
  `plan_checksum`, `plan_manifest`, `phase_started_at`, started/completed
  timestamps.
- Indexes:
  - btree `(s2_source_release_id, pt3_source_release_id, selector_version, started_at DESC)`
  - btree `(status, started_at DESC)`
  - partial unique `(advisory_lock_key)` for active runs

`corpus_selection_signals`

- Durable per-paper signal ledger keyed to one selection run.
- Columns: `corpus_id`, `phase_name`, `signal_kind`, `family_key`,
  `match_key`, `match_value`, `signal_count`, corpus/mapped contribution
  flags, `detail JSONB`, `created_at`.
- Indexes:
  - btree `(corpus_selection_run_id, corpus_id)`
  - btree `(corpus_id, corpus_selection_run_id)`
  - btree `(signal_kind, corpus_selection_run_id)`

#### `solemd.corpus_selection_artifacts` + `solemd.corpus_selection_chunks`

Operational ledgers for the optimized selector execution layer. These are
logged control tables. The large derived rollups they reference live in
`solemd_scratch` as UNLOGGED run-scoped tables.

`corpus_selection_artifacts`

- One row per run artifact kind:
  `paper_scope`, `entity_aggregate`, `relation_aggregate`,
  `mapped_entity_detail`, `mapped_relation_detail`.
- Columns: source release ids, selector version, phase name, artifact kind,
  storage schema/table, `is_logged`, lifecycle status, plan checksum, row and
  byte counts, `detail JSONB`, timestamps, and failure message.
- Artifact `detail` currently records `build_and_index_seconds`,
  `grant_seconds`, and `analyze_seconds`; index timing is folded into builder
  timing because index creation happens inside artifact-specific SQL builders.
- Resume contract: a phase can skip only when its ledger row is `complete`, the
  plan checksum matches, and the referenced scratch table still exists.
- Retention contract: keep the latest
  `CORPUS_ARTIFACT_RETENTION_RUNS` selection runs for the same S2/PT3/selector
  pair and drop older scratch tables after publish.
- Indexes:
  - btree `(corpus_selection_run_id, status, artifact_kind)`
  - btree `(s2_source_release_id, pt3_source_release_id, selector_version,
    artifact_kind, created_at DESC)`

`corpus_selection_chunks`

- Logged mapped-materialization chunk ledger keyed by
  `(corpus_selection_run_id, phase_name, bucket_id)`.
- `bucket_id` is `hashtextextended(corpus_id::text, 0) %
  materialization_bucket_count`; `corpus_id` is the permanent canonical paper
  identity from `solemd.corpus`, not the run id.
- Columns: `bucket_count`, status, attempts, started/completed timestamps,
  per-surface `row_counts JSONB`, error message, and `updated_at`.
- Resume contract: prior `running` or retryable `failed` chunks are reset to
  `pending` at the start of the mapped materialization phase; chunks at
  `CORPUS_MATERIALIZATION_CHUNK_MAX_ATTEMPTS` remain failed and block the phase
  until the root cause is fixed. Workers claim pending buckets with
  `FOR UPDATE SKIP LOCKED`.
- Index:
  - btree `(corpus_selection_run_id, phase_name, status, bucket_id)`

#### `solemd.paper_selection_summary`

Compact per-paper summary refreshed from the signal ledger and release-scoped
PubTator counts. The table name stays `paper_selection_summary` in the first
wave, but the business semantics are broad corpus admission plus mapped
promotion plus evidence-wave ranking.

- PK `corpus_id`.
- Columns: owning `corpus_selection_run_id`, `selector_version`,
  `current_status`, `primary_admission_reason`, `normalized_venue`,
  `publication_year`, corpus-admission booleans
  (`has_journal_match`, `has_pattern_match`, `has_vocab_entity_match`),
  mapped-rule booleans
  (`has_mapped_journal_match`, `has_mapped_pattern_match`,
  `has_mapped_entity_match`, `has_mapped_relation_match`), signal counts,
  release-scoped entity/relation counts, `mapped_family_keys`,
  locator-readiness booleans (`has_open_access`, `has_pmc_id`,
  `has_locator_candidate`, `has_abstract`), citation/reference support
  (`reference_out_count`, `influential_reference_count`), mapped/evidence
  priority scores, `updated_at`.
- Indexes:
  - btree `(current_status, corpus_id)`
  - btree `(corpus_selection_run_id, current_status, corpus_id)`
  - btree `(corpus_selection_run_id, current_status,
    evidence_priority_score DESC, mapped_priority_score DESC, corpus_id)` for
    mapped-paper evidence ranking
  - btree `(corpus_selection_run_id, current_status, publication_year DESC,
    has_locator_candidate, evidence_priority_score DESC, corpus_id)` for the
    current evidence-wave scan

Current first-wave summary fields that downstream waves should treat as the
stable contract:

- `publication_year`
- `has_locator_candidate`
- `has_mapped_pattern_match`
- `has_mapped_entity_match`
- `has_mapped_relation_match`
- `mapped_entity_signal_count`
- `mapped_relation_signal_count`
- `mapped_priority_score`
- `evidence_priority_score`

#### `solemd.corpus_wave_runs` + `solemd.corpus_wave_members`

Mapped-paper child-wave dispatch ledger for downstream mapped-rollout and
evidence enqueue. The current first-wave runtime dispatches through
`evidence.acquire_for_paper`.

`corpus_wave_runs`

- One row per `(published selection run, wave_policy_key, max_papers)` plan.
- The first locked policy key is `evidence_missing_pmc_bioc`.
- Columns: advisory lock, owning selection run, wave policy, requester,
  lifecycle status, error, `phases_completed`, `last_completed_phase`,
  `plan_checksum`, `plan_manifest`, `phase_started_at`, started/completed
  timestamps.
- Indexes:
  - btree `(corpus_selection_run_id, wave_policy_key, started_at DESC)`
  - btree `(status, started_at DESC)`
  - partial unique `(advisory_lock_key)` for active waves

`corpus_wave_members`

- PK `(corpus_wave_run_id, corpus_id)`.
- Columns: deterministic `member_ordinal`, persisted `priority_score`,
  `selection_detail JSONB`, `enqueued_at`, `actor_name`.
- Current first-wave semantics: one row per mapped paper selected by the
  recent/high-signal/locator-aware evidence-wave policy, copied from
  `paper_selection_summary` at wave-selection time so enqueue/resume does not
  have to recompute ranking.
- Indexes:
  - unique `(corpus_wave_run_id, member_ordinal)`
  - btree `(corpus_wave_run_id, enqueued_at, member_ordinal)` for pending
    dispatch scans

Locked first-wave evidence-wave policy carried in `plan_manifest` for
`wave_policy_key = 'evidence_missing_pmc_bioc'`:

- parent scope `current_status = 'mapped'`
- missing active `pmc_bioc` document
- `publication_year IS NULL OR publication_year >= current_year - 10`
- `evidence_priority_score >= 150`
- `has_locator_candidate = true`

### 4.4 Canonical paper facts & aggregates

Heavy per-paper fact tables that power graph, RAG priors, and API
detail. Narrow edge tables; heavy context text in a sibling table so
edge scans stay cheap.

Under the locked next stage contract, these heavier fanout tables are not
corpus-baseline requirements for every admitted paper. `paper_selection_summary`
remains the pre-mapped gating surface; `paper_authors` and actual
paper-to-paper `paper_citations` edges are mapped-owned enrichments.

#### `solemd.paper_citations`

Mapped-owned actual paper-to-paper citation edges. Fillfactor 100.

```sql
CREATE TABLE IF NOT EXISTS solemd.paper_citations (
    corpus_id BIGINT NOT NULL REFERENCES solemd.corpus (corpus_id) ON DELETE CASCADE,
    reference_checksum TEXT NOT NULL,
    cited_corpus_id BIGINT REFERENCES solemd.corpus (corpus_id) ON DELETE SET NULL,
    cited_s2_paper_id TEXT,
    linkage_status SMALLINT NOT NULL DEFAULT 1,
    is_influential BOOLEAN NOT NULL DEFAULT false,
    intent_raw TEXT,
    PRIMARY KEY (corpus_id, reference_checksum)
);
```

Mapped ownership contract:

- Scope rows to mapped citing papers rather than all upstream S2 citing papers.
- Resolve `cited_corpus_id` when the cited paper is already canonical; otherwise
  keep `cited_s2_paper_id` plus `linkage_status`.
- Keep `intent_raw` and `is_influential` as S2 release provenance, not evidence
  grounding.
- In-text citation anchors stay in `paper_citation_mentions`, because they
  depend on parsed document text and canonical offsets.

Local indexes:

- PK `(corpus_id, reference_checksum)`.
- Btree `(cited_corpus_id, corpus_id)` where `cited_corpus_id IS NOT NULL` for
  reverse-citation lookups.
- Partial btree `(corpus_id, cited_corpus_id)` where `is_influential = true`.

#### `solemd.paper_citation_contexts`

Future/provisional context table for heavy citation-context text if mapped
citation evidence needs it. If added, keep it keyed by the mapped citing
`corpus_id`. Fillfactor 100.

Columns: `corpus_id`, `cited_corpus_id`, `context_ordinal` INTEGER,
`section_role` SMALLINT, `context_text` TEXT (EXTENDED + lz4),
`context_intent` SMALLINT.

Local indexes:
- PK `(corpus_id, cited_corpus_id, context_ordinal)`.
- Btree `(corpus_id, cited_corpus_id)` — context fetch for an edge.

#### `solemd.paper_concepts`

Hash-partitioned by `corpus_id` × 32. Fillfactor 100.

Columns: `corpus_id`, `concept_id`,
`total_mention_count` INTEGER, `title_mention_count` INTEGER,
`abstract_mention_count` INTEGER, `fulltext_mention_count` INTEGER,
`source_mask` BIT(8), `first_offset` INTEGER, `last_offset` INTEGER.

Local indexes:
- PK `(corpus_id, concept_id)`.
- Reverse btree `(concept_id, corpus_id)` — concept → papers scans.

Replaces the current broad `entity_corpus_presence` style table.

#### `solemd.paper_relations`

Hash-partitioned by `corpus_id` × 32.

Columns: `corpus_id`, `subject_concept_id`, `relation_type` SMALLINT,
`object_concept_id`, `relation_count` INTEGER, `source_mask` BIT(8).

Local indexes:
- PK `(corpus_id, subject_concept_id, relation_type, object_concept_id)`.
- Reverse btree `(subject_concept_id, relation_type, object_concept_id, corpus_id)`.

#### `solemd.paper_metrics`

One row per paper. Fillfactor 90 (refreshed periodically).

Columns: `corpus_id` PK FK,
`citation_in_count` INTEGER, `citation_out_count` INTEGER,
`citation_velocity_3y` REAL, `influential_citation_count` INTEGER,
`concept_count` INTEGER, `relation_count` INTEGER,
`publication_age_years` SMALLINT, `evidence_recency_bucket` SMALLINT,
`evidence_priority_score` REAL,
`text_availability_flags` SMALLINT, `grounding_readiness_flags` SMALLINT,
`graph_readiness_flags` SMALLINT,
`historical_foundation_exception` BOOLEAN NOT NULL DEFAULT false,
`historical_exception_reason` SMALLINT.

Indexes:
- PK.
- Btree `(evidence_recency_bucket, evidence_priority_score DESC, corpus_id)`
  — evidence-wave admission scan.
- Partial btree `(historical_exception_reason, corpus_id)` where
  `historical_foundation_exception = true`.

#### `solemd.paper_top_concepts`

Narrow per-paper top-N concept cache. Fillfactor 100.

Columns: `corpus_id`, `rank` SMALLINT, `concept_id`,
`weight` REAL, `mention_count` INTEGER.

- PK `(corpus_id, rank)`. Reverse btree `(concept_id, corpus_id)`.

### 4.5 Canonical grounding & lineage

The authoritative offset / anchor / packet-assembly substrate. Every
serving projection dereferences through this family.

#### `solemd.paper_documents`

Fillfactor 90.

Columns: `corpus_id` PK FK, `document_source_kind` SMALLINT,
`source_priority` SMALLINT, `source_revision` TEXT, `text_hash` BYTEA,
`is_active` BOOLEAN NOT NULL DEFAULT true.

- PK `(corpus_id)`.
- Partial btree `(corpus_id, source_priority)` where `is_active = true`.

`document_source_kind` codes now include `pmc_bioc` as the targeted
evidence-acquisition full-text surface in addition to the existing
`pubtator_biocxml` and
`s2orc_annotation` codes.

#### `solemd.paper_text_contract_audit` (view)

Read-only warehouse audit surface for evidence-text contract checks.
This view exists so operators and later policy/audit passes can detect
document-spine vs `paper_text` mismatches without rebuilding the joins by hand.

Columns:
- `corpus_id`
- `has_active_document` BOOLEAN
- `document_source_kind` SMALLINT nullable
- `source_priority` SMALLINT nullable
- `source_revision` TEXT nullable
- `text_availability` SMALLINT
- `stored_abstract_present` BOOLEAN
- `parsed_abstract_present` BOOLEAN
- `section_count` INTEGER
- `block_count` INTEGER
- `sentence_count` INTEGER
- `abstract_block_count` INTEGER
- `retrieval_default_block_count` INTEGER
- `blocks_without_sentences_count` INTEGER
- `active_document_text_availability_mismatch` BOOLEAN
- `parsed_abstract_storage_mismatch` BOOLEAN

Primary use:
- flag rows where an active parsed document exists but
  `paper_text.text_availability` still reports less than full-text
- flag rows where parsed abstract structure exists but `paper_text.abstract`
  is still empty
- expose compact document-structure counts for evidence-text artifact review

#### `solemd.paper_sections`

Fillfactor 100 (append-only once parsed).

Columns: `corpus_id`, `section_ordinal` INTEGER,
`parent_section_ordinal` INTEGER, `section_role` SMALLINT,
`numbering_token` TEXT, `display_label` TEXT.

- PK `(corpus_id, section_ordinal)`.
- Btree `(corpus_id, section_role, section_ordinal)` — role-filtered scans.

#### `solemd.paper_blocks` (hash × 32)

Fillfactor 100.

Columns: `corpus_id`, `block_ordinal` INTEGER, `section_ordinal` INTEGER,
`start_offset` INTEGER, `end_offset` INTEGER,
`block_kind` SMALLINT, `section_role` SMALLINT (denormalized),
`is_retrieval_default` BOOLEAN NOT NULL DEFAULT true,
`linked_asset_ref` TEXT, `text` TEXT (EXTENDED + lz4).

Local indexes per partition:
- PK `(corpus_id, block_ordinal)`.
- Btree `(corpus_id, section_ordinal, block_ordinal)` — section-ordered
  scan for packet assembly.

#### `solemd.paper_sentences` (hash × 32)

Fillfactor 100.

Columns: `corpus_id`, `block_ordinal` INTEGER,
`sentence_ordinal` INTEGER, `section_ordinal` INTEGER,
`start_offset` INTEGER, `end_offset` INTEGER,
`segmentation_source` SMALLINT, `text` TEXT (EXTENDED + lz4).

Local indexes:
- PK `(corpus_id, block_ordinal, sentence_ordinal)`.
- Btree `(corpus_id, sentence_ordinal)` — flat sentence-ordered scan.

#### `solemd.paper_citation_mentions` (hash × 32)

Fillfactor 100.

Columns: `corpus_id`, `matched_corpus_id`,
`canonical_section_ordinal` INTEGER, `canonical_block_ordinal` INTEGER,
`canonical_sentence_ordinal` INTEGER,
`source_start_offset` INTEGER, `source_end_offset` INTEGER,
`span_origin` SMALLINT, `alignment_status` SMALLINT,
`source_system` SMALLINT, `source_revision` TEXT,
`source_document_key` TEXT, `source_plane` SMALLINT,
`source_citation_key` TEXT, `text` TEXT.

Local indexes:
- PK `(corpus_id, canonical_block_ordinal, canonical_sentence_ordinal, source_citation_key)`.
- Btree `(corpus_id, matched_corpus_id)` — citation-grounded packet
  assembly.
- Btree `(matched_corpus_id, corpus_id)` — reverse "who cites X in what
  span."

#### `solemd.paper_entity_mentions` (hash × 32)

Fillfactor 100.

Columns: `corpus_id`, `concept_id`,
`canonical_section_ordinal` INTEGER, `canonical_block_ordinal` INTEGER,
`canonical_sentence_ordinal` INTEGER,
`source_start_offset` INTEGER, `source_end_offset` INTEGER,
`entity_type` SMALLINT, `span_origin` SMALLINT,
`alignment_status` SMALLINT, `source_system` SMALLINT,
`source_revision` TEXT, `source_document_key` TEXT,
`source_plane` SMALLINT, `concept_namespace` SMALLINT,
`concept_id_raw` TEXT, `text` TEXT.

Local indexes:
- PK `(corpus_id, canonical_block_ordinal, canonical_sentence_ordinal, concept_id_raw)`.
- Btree `(corpus_id, concept_id)` — entity-grounded packet assembly.
- Btree `(concept_id, corpus_id)` — reverse lookup.

#### `solemd.paper_chunk_versions`

Fillfactor 80.

Columns: `chunk_version_key` UUID PK default `uuidv7()`,
`policy_key` TEXT NOT NULL, `created_at` TIMESTAMPTZ,
`is_active` BOOLEAN NOT NULL DEFAULT false,
`is_default` BOOLEAN NOT NULL DEFAULT false,
`notes` TEXT.

- Partial unique index `(is_active)` where `is_active = true` enforcing
  a single active version.
- Partial unique index `(is_default)` where `is_default = true`
  enforcing a single default.

#### `solemd.paper_chunk_members` (hash × 32)

Fillfactor 100.

Columns: `corpus_id`, `chunk_id` BIGINT,
`chunk_version_key` UUID, `member_ordinal` SMALLINT,
`member_block_ordinal` INTEGER, `member_sentence_ordinal` INTEGER.

Local indexes:
- PK `(corpus_id, chunk_id, member_ordinal)`.
- Reverse btree `(corpus_id, member_block_ordinal)` — block → chunk.

#### `solemd.paper_chunks` (hash × 32)

Compatibility / fallback. Fillfactor 100.

Columns: `corpus_id`, `chunk_id` BIGINT,
`chunk_version_key` UUID,
`start_block_ordinal` INTEGER, `end_block_ordinal` INTEGER,
`start_sentence_ordinal` INTEGER, `end_sentence_ordinal` INTEGER,
`text` TEXT (EXTENDED + lz4).

Local indexes:
- PK `(corpus_id, chunk_id)`.
- GIN on `to_tsvector('english', text)` only if PG fallback lexical
  retrieval is still enabled (provisional; remove if unused).

#### `solemd.paper_evidence_units` (not partitioned; provisional on scale)

New table introduced here to make `evidence_key` round-trippable from
OpenSearch serving docs back to canonical grounding, without the
serving plane needing to know canonical coordinates.

Columns:
- `evidence_key` UUID PK (UUIDv5, content-bound — see §2)
- `corpus_id` BIGINT FK → `corpus`
- `chunk_version_key` UUID FK → `paper_chunk_versions`
- `evidence_kind` SMALLINT (enum: `paragraph` | `results_paragraph` |
  `abstract_conclusion` | `sentence_window`)
- `section_ordinal` INTEGER
- `block_ordinal` INTEGER
- `sentence_start_ordinal` INTEGER
- `sentence_end_ordinal` INTEGER
- `section_role` SMALLINT (denormalized from `paper_sections` for
  filter-without-join)
- `derivation_revision` SMALLINT

Indexes:
- PK `(evidence_key)` — round-trip from serving.
- Btree `(corpus_id, chunk_version_key, block_ordinal, sentence_start_ordinal)`
  — inverse lookup for packet assembly.
- Btree `(chunk_version_key, corpus_id)` — version-scoped sweeps.

Partitioning: deferred. If row count grows past ~50 M the table partitions
by `hashtext(evidence_key::text)` × 32, since the primary access pattern
is evidence_key-keyed (corpus_id-keyed is secondary).

### 4.6 Canonical graph embeddings

Graph-build embeddings live in their own table so `papers` / `paper_text`
stays narrow. **Retrieval embeddings are not stored in warehouse PG** —
OpenSearch is the runtime owner per `rag-future.md`, and reproducibility
comes from release-scoped archive artifacts on `/mnt/solemd-graph`
(see `01-storage.md` §4), not from a relational table.

#### `solemd.paper_embeddings_graph`

- `corpus_id` BIGINT
- `model_key` SMALLINT (e.g. `SPECTER2_v2 = 1`)
- `embedding_revision` SMALLINT
- `embedding_source_kind` SMALLINT (`upstream_release = 1`, `local_generated = 2`)
- `embedding` halfvec(768)

- PK `(corpus_id, model_key)`. No ANN index by default — SPECTER2 powers
  graph build (cuML / cuGraph) and relatedness priors, not runtime
  retrieval.

Rule: this is a warehouse-side graph-build / audit table only. ANN
indexing stays on OpenSearch for runtime dense retrieval; graph build
runs its own ANN outside PG via cuML / cuGraph. If a PG-local
comparison path is ever justified, HNSW on this table is the trigger
already listed in the deferred decisions.

Provenance rule: graph embeddings must be distinguishable as upstream-provided
versus locally generated. `model_key` + `embedding_revision` name the model
family/version; `embedding_source_kind` records whether the row came from an
upstream release shard or from local generation during a rollout wave.

### 4.7 Graph build control

Warehouse-side build lineage. Serve-facing graph metadata lives on the
serve cluster (`03-serve-schema.md`).

#### `solemd.graph_runs`

Fillfactor 80.

Columns: `graph_run_id` UUID PK default `uuidv7()`,
`built_at` TIMESTAMPTZ, `published_at` TIMESTAMPTZ,
`layout_policy_version` TEXT, `embedding_model_key` SMALLINT,
`status` SMALLINT (running | succeeded | failed | published | retired),
`source_release_watermark` INTEGER, `qa_summary` JSONB (small).

- PK. Btree `(status, built_at DESC)`.

#### `solemd.graph_bundle_artifacts`

Fillfactor 80.

Columns: `graph_run_id`, `artifact_kind` SMALLINT,
`artifact_uri` TEXT, `artifact_checksum` TEXT, `row_count` BIGINT,
`published_at` TIMESTAMPTZ.

- PK `(graph_run_id, artifact_kind)`.

## 5. Cross-family invariants

Enforced by a mix of foreign keys, generated columns, and scheduled
audit queries in `pg_cron`.

1. `corpus_id` present on every fact, grounding, concept-attribution,
   and embedding row joins back to exactly one `solemd.corpus` row.
2. `evidence_key` in `paper_evidence_units` is deterministic from
   `(corpus_id, evidence_kind, section_ordinal, block_ordinal,
   sentence_start_ordinal, sentence_end_ordinal, chunk_version_key)`.
   A scheduled audit recomputes the hash and asserts equality.
3. `concept_id_raw` on PubTator / mention rows maps to exactly one
   `concept_id` in `concepts` via `concept_xrefs` once the backfill is
   complete. Unmapped rows carry `concept_id IS NULL` (not a sentinel
   value).
4. `source_release_id` values on facts are all `<=` the
   `last_seen_release_id` on `paper_lifecycle.corpus_id`.
5. `paper_text.fts_vector` is always recomputed from current
   `(title, abstract)` — enforced by `GENERATED ALWAYS AS … STORED`,
   not a trigger.

## 6. Write patterns

### 6.1 Bulk ingest window

Per `05-ingest-pipeline.md`:
1. Target table set to `UNLOGGED` for the run.
2. Autovacuum disabled on target partitions.
3. Indexes dropped (or created on empty table then data loaded — PG 18
   permits both; dropping is cheaper at TB scale).
4. DuckDB transforms parquet → binary COPY, fans out to 32
   per-partition asyncpg `copy_records_to_table` streams.
5. `CREATE INDEX` (parallel, not `CONCURRENTLY`) using
   `maintenance_work_mem=16GB`, `max_parallel_maintenance_workers=12`.
6. `ALTER TABLE … SET LOGGED` per partition.
7. `VACUUM (FREEZE, ANALYZE, PARALLEL 6)`.
8. Autovacuum re-enabled with per-table thresholds below.

### 6.2 Incremental write

- Canonical metadata (`papers`, `paper_text`, `paper_lifecycle`,
  `concepts`): `INSERT … ON CONFLICT … DO UPDATE` keyed on the natural
  identity.
- Fact tables (`paper_citations`, `paper_concepts`): incremental loads
  stage into `_next` partition-matching tables, then `ATTACH PARTITION`
  replaces the stale month. No in-place UPDATE of fact rows.
- Control tables (`source_releases`, `ingest_runs`, `graph_runs`):
  small UPDATE traffic; autovacuum default.

### 6.3 Per-table autovacuum overrides

Append-mostly large facts:
```sql
ALTER TABLE solemd.paper_citations SET (
  autovacuum_vacuum_scale_factor = 0.2,
  autovacuum_vacuum_insert_scale_factor = 0.5,
  autovacuum_analyze_scale_factor = 0.1
);
```

Text-bearing grounding partitions: same as above plus
`autovacuum_vacuum_cost_delay = 2` so vacuum doesn't starve ingest.

Control tables:
```sql
ALTER TABLE solemd.source_releases SET (
  autovacuum_vacuum_scale_factor = 0.0,
  autovacuum_vacuum_threshold = 50
);
```

## 7. Read patterns

### 7.1 From the serve cluster (engine API)

- Bounded grounding dereference over `postgres_fdw`. Permitted targets
  and key shapes are in `00-topology.md` §4. Never a full-table scan
  across FDW.

### 7.2 From warehouse-local projection jobs

- Large hash-joins over `paper_citations` / `paper_entity_mentions` /
  `pubtator.*` happen here, not on serve.
- Every projection job reads warehouse + writes serve (staging table +
  swap). Projection jobs never write warehouse beyond their own control
  rows in `api_projection_runs` (on serve).

### 7.3 Analytical queries

- Ad-hoc analytical runs (benchmark evaluation, concept drift audit)
  execute on warehouse directly through the admin pool. Never through
  the serving path.

## 8. Decisions — locked / provisional / deferred

### Locked now

| Decision | Rationale |
|---|---|
| Three schemas only: `solemd`, `pubtator`, `umls` | Fewer schemas = fewer search-path issues; raw-ingest tables live under `solemd.s2_*_raw` without a separate `raw` schema. |
| BIGINT `corpus_id` with identity sequence | Single-writer single-DB; tight join keys; cannot be undone after ingest. |
| UUIDv7 for `ingest_run_id`, `graph_run_id`, `chunk_version_key` | Timestamp-ordered, externally sortable, no sequence leakage. |
| UUIDv5 for `evidence_key`, stored in `paper_evidence_units` | Content-bound, release-independent, round-trippable from serving docs. |
| Hash partitioning by `corpus_id` × 32 for the grounding + entity families | Matches rag-future.md §4 shape; stays inside PG 18 fast-path lock slots. `paper_citations` is mapped-owned and unpartitioned until the §11 trigger fires. |
| `paper_text` split out from `papers` | Metadata reads don't drag abstracts off disk. |
| `paper_citation_contexts` split out from `paper_citations` | Narrow edge, heavy text separated. |
| `paper_embeddings_graph` kept; `paper_embeddings_retrieval` omitted from day-one warehouse | SPECTER2 powers graph build; retrieval embeddings are owned by OpenSearch. Reproducibility comes from release-scoped archive artifacts, not a hot PG table. Provenance on graph embeddings stays explicit via `embedding_source_kind`. |
| `paper_evidence_units` lives on warehouse, exposed to serve via FDW | Derives from and joins canonical grounding directly; serve consumes through the bounded FDW path in `00-topology.md` §4. |
| Generated columns for `normalized_title_key`, `normalized_preferred_name`, `normalized_alias`, `fts_vector` | Always consistent with source; no trigger required. |
| `TIMESTAMPTZ` everywhere, `text` everywhere, `jsonb` everywhere | Modern PG defaults. |
| SMALLINT-code enums sourced from `db/schema/enum-codes.yaml` machine-readable registry | `engine/app/models/shared/enums.py` and SQL column comments both generate from the registry; the registry is authoritative, code and comments are derived. |
| `COMMENT ON` every table and non-obvious column | Tooling legibility. |
| No `postgres_fdw` installed on warehouse | Warehouse is source, never querier. |

### Provisional (revisit after schema draft + sample build)

| Decision | Revisit trigger |
|---|---|
| 32-way hash partitioning | Measure partition-lock contention under real 32-way bulk COPY. If clean, keep; if 16 is plenty, shrink. |
| `halfvec(768)` | Confirm recall parity against `vector(768)` on a sample HNSW build once the serving plane is wired. |
| lz4 TOAST on every text column | Measure against raw / pglz on the widest columns (`paper_citation_contexts.context_text`, `paper_blocks.text`). |
| Fillfactor tiers (100 / 90 / 80) | Revisit after first projection-upsert benchmarks. |
| Partial indexes on lifecycle flags | Measure planner uptake; drop any that don't get chosen. |
| BRIN on `ingest_runs.started_at` | Effective only if physical order stays correlated after restores; verify post-restore. |
| `paper_evidence_units` unpartitioned | Partition by `hashtext(evidence_key::text)` × 32 if row count exceeds ~50 M. |
| `paper_chunks` GIN fallback | Remove entirely if the PG fallback retrieval path is retired. |
| `papers (venue_id, year)` composite index | Drop unless the query family proves to use it. |
| `MAXALIGN` ordering confirmed via `pg_column_size` sampling | Already applied in the schema files — verify no accidental misordering after code-gen. |
| `CREATE STATISTICS` on correlated filters (e.g. `paper_citations (intent_code, is_influential, source_mask)`) | Add if planner estimates are off. |

### Deferred (trigger-gated)

| Decision | Trigger |
|---|---|
| `pg_partman` 5.x for `ingest_runs` / `load_history` time-range lifecycle | Row count > 10 M. |
| HNSW on `paper_embeddings_graph` inside PG | A PG-local graph-build query family proves to need it. |
| Columnar extension (Citus / Hydra) on `paper_citation_contexts` | Table > 200 GB and scan-heavy. |
| Citation edge table partitioning added for `paper_citations` | Mapped citation volume or reverse-citation workload outgrows the current btrees. |
| `concept_aliases` trigram index | Fuzzy alias lookup becomes a proven hot path. |
| Staged list-partitioning on `pubtator.*` before `corpus_id` backfill | If `pmid → corpus_id` backfill can't complete in a single pass. |
| `paper_evidence_units` partitioning | Row count > ~50 M. |
| Moving `umls.*` to a read-only replica of a shared UMLS volume | If UMLS reload pressure becomes a contention issue with other warehouse writes. |

## Open items before `03-serve-schema.md`

All three previously open items are resolved and reflected in §8 above:

- **Enum source of truth** — canonical machine-readable registry at
  `db/schema/enum-codes.yaml`. `engine/app/models/shared/enums.py` and
  SQL column comments both generate from the registry. Neither code nor
  comments are authoritative. Generator wiring is specified in
  `12-migrations.md`.
- **`paper_evidence_units` placement** — warehouse. Serve consumes it
  via the bounded FDW path already defined in `00-topology.md` §4. If
  FDW round-trip latency ever becomes painful, add a slim projected
  cache on serve; do not move the canonical table.
- **`paper_embeddings_retrieval`** — omitted from the day-one warehouse
  schema. OpenSearch owns runtime dense retrieval. If reproducibility
  for retrieval embeddings is ever needed, archive them as release-scoped
  artifacts under `/mnt/solemd-graph/archives/` (or MinIO when we stand
  it up), not as a relational table.

None of these block `03-serve-schema.md`.
